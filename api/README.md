

to seed database with sample data


Install the TypeScript executor if you haven't already:

npm install -D tsx

then run 

npx prisma db seed

reset and seed database in one command

npx prisma migrate reset
